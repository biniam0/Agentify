version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentx-backend-local
    ports:
      - "4000:4000"
    env_file:
      - .env
    command: sh -c "npx prisma migrate deploy && node dist/server.js"